services:
  telegram-summary-bot:
    image: ${IMAGE:-telegram-summary-bot:local}
    build: .
    container_name: telegram-summary-bot
    restart: unless-stopped
    env_file:
      - ${ENV_FILE:-.env}
    volumes:
      - ./data:/app/data
